# Optimization Archive

This directory preserves the old long-form optimization documents.

Do not open these files by default.

Use them only when:

1. a compact document in the parent directory explicitly fails to answer a
   specific historical question,
2. you must reconstruct a prior experiment or artifact path,
3. or you are redesigning an area whose compact summary intentionally omits
   detail.
